name: rides_repository

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.13.1
  rxdart: ^0.23.1
  equatable: ^1.1.0
  firebase_core: ^0.4.3